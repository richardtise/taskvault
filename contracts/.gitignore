# Foundry
/cache/
/out/
# NOTE: /lib/ is intentionally NOT ignored. Dependencies are committed as git
# submodules (see .gitmodules) so `forge build` is reproducible from a clean clone.

# Env
.env

# IDE
.vscode/
.idea/

# OS
.DS_Store
